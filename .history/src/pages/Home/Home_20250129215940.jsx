import React from "react";

import "./styles.scss";

const  Home = async () => {
    
const result = await model.generateContent(prompt);

  return (
    <div>
        <input type="text" />
    </div>
  );
};

export default Home;